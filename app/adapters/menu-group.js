import ApplicationAdapter from './application';
import BatchUpdatesAdapterMixin from '../mixins/batch-updates-adapter-mixin';

export default ApplicationAdapter.extend(BatchUpdatesAdapterMixin, {
});
